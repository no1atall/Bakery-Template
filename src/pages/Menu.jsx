import React from "react";
import styled from "styled-components";
import tw from "tailwind.macro";


const Wrapper = styled.div`
  ${tw`bg-gray-900 h-screen`}
`


const Menu = () => {
  return <Wrapper>Menu</Wrapper>;
};

export default Menu;
